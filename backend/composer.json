{
    "name": "davidaugusto89/gestor-academico",
    "description": "Sistema acadêmico com PHP puro + Quasar frontend",
    "type": "project",
    "scripts": {
        "test": "phpunit",
        "test:coverage": "XDEBUG_MODE=coverage ./vendor/bin/phpunit --coverage-html coverage-report",
        "test:coverage:xml": "XDEBUG_MODE=coverage ./vendor/bin/phpunit --coverage-clover coverage-report/coverage.xml",
        "test:coverage:all": "XDEBUG_MODE=coverage ./vendor/bin/phpunit --coverage-html coverage-report --coverage-clover coverage-report/coverage.xml"
    },
    "require": {
        "firebase/php-jwt": "^6.9",
        "vlucas/phpdotenv": "^5.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/",
            "Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^12.1"
    }
}